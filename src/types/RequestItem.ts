export interface RequestItem {
  id: number
  name: string
  reason: string
  purpose: string
  quantity: number
  unitPrice: number
  amount: number
  supplierId: string
  status: string
  approval: string
}